apiVersion: v1
kind: Service
metadata:
  name: {{ include "prd-cmc.fullname" . }}
  labels:
    {{- include "prd-cmc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prd-cmc.selectorLabels" . | nindent 4 }}